# dsh-collab-shell

Pluginmax collaboration shell. It replaces the upstream app frame, keeps the
native conversation, workspace browser, settings shell, and meeting overlay,
and adds global workbench, task, and workflow destinations.
